@extends('layouts.app')
@section('title','My Profile — TAPGO Travel')
@section('content')<main class="marketplace-page"><div class="container"><x-breadcrumb current="My profile"/><div class="row g-4"><aside class="col-lg-3"><div class="profile-nav"><div class="profile-avatar">{{ strtoupper(substr(Auth::user()->name, 0, 1)) }}</div><strong>{{ Auth::user()->name }}</strong><small>{{ Auth::user()->email }}</small><hr>@foreach(['Profile','My Bookings','Saved Hotels','Saved Destinations','Reviews','Payment Methods','Notifications','Settings'] as $item)<a class="{{ $loop->first ? 'active' : '' }}" href="#">{{ $item }}</a>@endforeach<form method="POST" action="{{ route('logout') }}">@csrf<button>Log out</button></form></div></aside><div class="col-lg-9"><h1>Good to see you, {{ explode(' ', Auth::user()->name)[0] }}.</h1><p class="text-muted mb-4">Here is a quick look at your TAPGO journey.</p><div class="row g-3 mb-4">@foreach([['0','Upcoming trips'],['0','Completed trips'],['0','Saved places'],['0','Reviews']] as [$value,$label])<div class="col-sm-3"><div class="profile-stat"><strong>{{ $value }}</strong><span>{{ $label }}</span></div></div>@endforeach</div><section class="detail-panel"><div class="d-flex justify-content-between align-items-center"><div><p class="eyebrow">Upcoming booking</p><h2 class="h4">Nothing booked just yet</h2><p class="text-muted mb-0">Your confirmed journeys will appear here.</p></div><a href="{{ route('destinations.index') }}" class="btn btn-primary">Explore trips</a></div></section><section class="detail-panel"><div class="d-flex justify-content-between"><div><h2 class="h4">Profile details</h2><p class="mb-1"><strong>{{ Auth::user()->name }}</strong></p><p class="text-muted mb-0">{{ Auth::user()->email }}</p></div><button class="btn btn-outline-primary btn-sm">Edit profile</button></div></section></div></div></div></main>@endsection
