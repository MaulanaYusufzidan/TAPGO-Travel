@props(['trip'])
@php $fallback = 'https://images.unsplash.com/photo-1530789253388-582c481c54b0?auto=format&fit=crop&w=900&q=85'; $image = $trip->images->first()?->image_path ? asset('storage/'.$trip->images->first()->image_path) : $fallback; @endphp
<article class="travel-card h-100"><a href="{{ route('trips.show', $trip) }}" class="travel-card__image"><img src="{{ $image }}" alt="{{ $trip->title }}" onerror="this.onerror=null;this.src='{{ $fallback }}'">@if($trip->is_featured)<span class="card-label">Handpicked</span>@endif</a><div class="travel-card__body"><div class="d-flex justify-content-between gap-2 align-items-center mb-2"><span class="small text-muted">{{ $trip->destination->name }} · {{ $trip->duration }}</span><span class="rating">★ {{ number_format($trip->rating_avg, 1) }}</span></div><h3 class="h5 mb-2"><a href="{{ route('trips.show', $trip) }}">{{ $trip->title }}</a></h3><p class="text-muted small mb-3">{{ \Illuminate\Support\Str::limit($trip->description, 76) }}</p><div class="d-flex justify-content-between align-items-end border-top pt-3"><span class="small text-muted">From <strong class="d-block price">Rp {{ number_format($trip->base_price, 0, ',', '.') }}</strong></span><a class="btn btn-outline-primary btn-sm" href="{{ route('trips.show', $trip) }}">View trip</a></div></div></article>
